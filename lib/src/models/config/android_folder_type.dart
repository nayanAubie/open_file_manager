/// Available folder types for Android.
enum AndroidFolderType {
  /// Recent folder
  recent,

  /// Download folder
  download,

  /// Other folder, need to specify `folderPath` in `AndroidConfig`
  other,
}
