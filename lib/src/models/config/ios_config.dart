/// Configuration class for iOS platform.
final class IosConfig {
  /// Folder path to open specific folder inside app's document folder.
  /// Path is case sensitive.
  final String? folderPath;

  IosConfig({this.folderPath});
}
